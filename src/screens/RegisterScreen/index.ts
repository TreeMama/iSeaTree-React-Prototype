/* This line of code is exporting all the named exports from the `RegisterScreen` module. It means that
any other module that imports this module will have access to all the named exports from the
`RegisterScreen` module. */

export * from './RegisterScreen'
