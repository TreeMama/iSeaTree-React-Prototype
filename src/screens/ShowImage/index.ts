/* `export * from './ShowImage'` is exporting all the named exports from the `ShowImage` module. This
means that any other module that imports from this module will have access to all the named exports
from `ShowImage`. */

export * from './ShowImage'
