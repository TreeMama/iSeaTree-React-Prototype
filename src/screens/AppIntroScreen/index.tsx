/* This code is importing the `AppIntroScreen` component from the file `./AppIntroScreen` and then
exporting it as the default export of this module. This allows other modules to import this
component using `import AppIntroScreen from 'path/to/module'` syntax. */

import AppIntroScreen from './AppIntroScreen'
export default AppIntroScreen
